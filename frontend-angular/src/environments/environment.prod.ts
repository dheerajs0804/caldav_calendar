export const environment = {
  production: true,
  apiUrl: 'https://your-production-api.com',  // Update with your production API URL
  logLevel: 'info',  // Only info, warn, error in production
  enableLogging: true
};
